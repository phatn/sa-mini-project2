spring:
  datasource:
    url: jdbc:mysql://localhost:3306/citizix_db
    username: citizix_user
    password: An0thrS3crt
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
server:
  port: 8080
